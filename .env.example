# 서버(Vercel 서버리스 함수) 전용 — 클라이언트 번들에 포함되지 않음.
# Vercel 대시보드 > Settings > Environment Variables 에 설정한다.
APP_PASSWORD=

# 클라이언트용 — API 기본 주소.
# 웹(Vercel 호스팅)에서는 비워두면 같은 출처 /api/words 를 호출한다.
# APK 빌드 시에만 .env.production 에 배포된 Vercel URL 을 넣는다.
VITE_API_BASE=
